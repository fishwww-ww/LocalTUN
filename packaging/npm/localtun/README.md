# @fishwww-ww/localtun

`localtun` 是一个 CLI 工具，用于通过 SSH 反向隧道，把远程服务器的代理流量转发到本地代理端口。

安装：

```bash
npm install -g @fishwww-ww/localtun
```

验证：

```bash
localtun --help
```

更多说明见项目主页：

- <https://github.com/fishwww-ww/LocalTUN>
